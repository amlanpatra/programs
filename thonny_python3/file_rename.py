#create a file from terminal by 'touch "check.txt"'

import os
def rename():
    
    a= 'date/time'
    b= 'dv'
    return (a+b)
# OS module doesn't accept any substituting variable, so a string must be provided
os.rename('rename()','check' )

#print(a)